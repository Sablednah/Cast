# Cast owns its mob bodies from spawn and rebuilds their goals outright.
# Both selectors are protected on Mob; ZombieMod carries the same two lines.
public net.minecraft.world.entity.Mob goalSelector
public net.minecraft.world.entity.Mob targetSelector
