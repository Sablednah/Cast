# Permissions and access

| Node | Default | What it allows |
|---|---|---|
| `cast.admin` | nobody; **op level 2 also passes** | the whole `/cast` tree |

Through NeoForge's `PermissionAPI`; SableCraft Standards and LuckPerms both
manage it. Players need nothing to see or right-click an NPC.
